import { Sour_Gummy } from 'next/font/google';
import { Geist } from 'next/font/google';
 
export const sour_gummy = Sour_Gummy({ subsets: ['latin'] });
export const geist = Sour_Gummy({ subsets: ['latin'] });